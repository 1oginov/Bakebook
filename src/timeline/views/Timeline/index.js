import Timeline from './Timeline';
import style from './Timeline.styles';
import tabBarIcon from './Timeline.tabBarIcon';

const view = style(Timeline);

view.navigationOptions = { tabBarIcon };

export default view;
