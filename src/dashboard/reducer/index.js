import * as T from '../actions/types';

/**
 * Initial state.
 * @type {{
 * }}
 */
const initialState = {
  //
};

/**
 * User reducer.
 * @param {Object} state
 * @param {Object} action
 * @return {Object}
 */
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
